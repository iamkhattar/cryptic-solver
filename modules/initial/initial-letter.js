/**
 *
 * @param word : word for which initial letter has to be generates
 * @return : initial letter
 *
 * getInitialLetter() returns the initial letter of the given word
 */
function getInitialLetter(word) {
  return word[0].toLowerCase();
}

module.exports = getInitialLetter;
